---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-vol-claim
  labels:
    placement: patient-portal-placement
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ocs-storagecluster-ceph-rbd   
